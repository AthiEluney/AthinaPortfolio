let progress = setInterval(() => {
    progressStarValue++;

    progressValue.textContent = '${progressStarValue}%';
    circularProgress.style.background = 'conic-gradient(#2aB3e8 ${progressStarValue * 3.6}deg, #ededed 0deg)';

    if ()
        ;


});
